<snippet>
  <tabTrigger>h_tokens(\$type,</tabTrigger>
  <content><![CDATA[/**
 * Implements hook_tokens(\$type, \$tokens, array \$data = array(), array \$options = array().
 */
function ${TM_FILEPATH/.+(?:\/|\\)(?:themes|modules)(?:\/|\\)([a-zA-Z0-9_]+)(?:\/|\\).*/$1/}_tokens(\$type, \$tokens, array \$data = array(), array \$options = array()) {
  \$replacements = array();
  ${1:/* Your code here */}

  return \$replacements;
}
]]></content>
</snippet>